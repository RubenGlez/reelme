name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  template:
    name: Template
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: cli/template/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        working-directory: cli/template

      - name: Typecheck
        run: pnpm typecheck
        working-directory: cli/template

      - name: Lint
        run: pnpm lint
        working-directory: cli/template

      - name: Test
        run: pnpm test
        working-directory: cli/template
